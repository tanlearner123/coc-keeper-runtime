# Phase 60-01 Plan: Admin Governance Actions

## Wave 1: Core Implementation

### Context
Phase 60 implements admin governance actions (AV-05 through AV-08) and debug_status (AUD-03).

**Key Decisions from discuss-phase:**
- D-01: Reuse existing `archive_profile()`/`retire_instance()` methods + add governance event with admin operator_id and reason
- D-02: Reason required (min 10 chars) for all admin actions
- D-03: Old owner becomes ADMIN after reassignment (not MEMBER)
- D-04: debug_status shows ALL events, limit 20

### Tasks

**Task 1 (TDD RED):** Add failing tests for admin governance methods
- Add `test_force_archive_instance_admin_*` tests to test_instance_management.py
- Add `test_reassign_ownership_*` tests to test_instance_management.py
- Add `test_force_archive_*` tests to test_archive_repository (existing test file)

**Task 2 (TDD GREEN):** Implement admin governance methods
- SessionStore: Add `force_archive_instance()` with admin_id/reason logging
- SessionStore: Add `reassign_ownership()` with role change + logging
- ArchiveRepository: Already has `archive_profile()` - just need to call + log

**Task 3:** Add Discord commands
- `admin_force_archive_profile` (profile_id, reason)
- `admin_force_archive_instance` (user_id, reason)  
- `admin_reassign_ownership` (user_id, reason)
- `debug_status` (no params)

### Files Modified
- `tests/test_instance_management.py` - Add failing tests
- `src/dm_bot/orchestrator/session_store.py` - Add force_archive_instance, reassign_ownership
- `src/dm_bot/discord_bot/commands.py` - Add 4 handler methods
- `src/dm_bot/discord_bot/client.py` - Add 4 command registrations

### Success Criteria
1. Admin can force-archive any active profile with required reason
2. Admin can force-archive any campaign instance with required reason
3. Owner can reassign ownership to another member (old owner → ADMIN)
4. All admin actions logged with timestamp, admin ID, target ID, reason
5. debug_status shows 20 most recent governance events

### Verification
```bash
uv run pytest -xvs tests/test_instance_management.py -k "force_archive_instance or reassign_ownership or force_archive"
uv run pytest -q
uv run python -m dm_bot.main smoke-check
```
