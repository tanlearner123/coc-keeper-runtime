# Plan 40-01: Contracts, Normalization Seams, and Backward Compatibility

## Goal

Establish stable Track B foundation contracts for builder-to-archive flow by introducing explicit typed schemas, an answer-normalization seam, and backward-compatible archive profile evolution without changing user-facing behavior more than necessary.

## Tasks

### Task 1: Add failing tests for normalization and backward-compatible archive evolution
<files>
- `tests/test_v18_archive_builder.py`
- `tests/test_persistence_store.py`
</files>
<verify>
- `uv run pytest tests/test_v18_archive_builder.py tests/test_persistence_store.py -q`
</verify>
<done>
- Tests describe normalized builder-answer handling, archive schema compatibility, and continued support for existing profile queries.
</done>

### Task 2: Define typed contracts for builder answers, normalized slots, and archive writeback payloads
<files>
- `src/dm_bot/coc/builder.py`
- `src/dm_bot/coc/archive.py`
</files>
<verify>
- `uv run pytest tests/test_v18_archive_builder.py -q`
</verify>
<done>
- Track B has explicit Pydantic or equivalent typed contracts for raw answers, normalized fields, and archive writeback payloads.
</done>

### Task 3: Implement an AnswerNormalizer seam with Chinese-friendly canonicalization
<files>
- `src/dm_bot/coc/builder.py`
- `tests/test_v18_archive_builder.py`
</files>
<verify>
- `uv run pytest tests/test_v18_archive_builder.py -q`
</verify>
<done>
- Builder input can be normalized into canonical slot values without changing the visible interview flow.
</done>

### Task 4: Add backward-compatible archive schema versioning/defaults
<files>
- `src/dm_bot/coc/archive.py`
- `tests/test_persistence_store.py`
</files>
<verify>
- `uv run pytest tests/test_persistence_store.py -q`
</verify>
<done>
- Existing profiles remain loadable, new fields are default-safe, and profile queries continue to work without migration breakage.
</done>

## Exit Criteria

- Track B has explicit contract boundaries between builder answers, normalized fields, and archive writeback.
- Archive schema can evolve without breaking existing persisted profiles.
- The next synthesis phase can target well-defined payloads instead of implicit dict conventions.
